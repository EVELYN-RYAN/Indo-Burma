@{
}
@model Cart
<a class="btn btn-sm btn-secondary navbar-btn" asp-page="/Cart"
   asp-route-returnurl="@ViewContext.HttpContext.Request.PathAndQuery()">
    <img src="https://img.icons8.com/external-kiranshastry-lineal-kiranshastry/32/000000/external-shopping-cart-interface-kiranshastry-lineal-kiranshastry.png" />
</a>
@if (Model.Items.Count() > 0)
{
    <small class="navbar-text">
        <b>Your cart:</b>
        @Model.Items.Sum(x => x.Quantity) item(s)
        @Model.CalculateTotal().ToString("c")
    </small>
}
